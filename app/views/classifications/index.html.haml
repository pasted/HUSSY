%h1 Listing classifications
%table
  %tr
    %th Id
    %th Name
    %td
    %td
    %td
  - @classifications.each do |classification|
    %tr
      %td= classification.id
      %td= classification.name
      %td= link_to 'Show', classification
      %td= link_to 'Edit', edit_classification_path(classification)
      %td= link_to 'Destroy', classification, :confirm => 'Are you sure?', :method => :delete
%br/
= link_to 'New Classification', new_classification_path
