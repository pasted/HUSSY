= form_for(@outcome) do |f|
  - if @outcome.errors.any?
    #error_explanation
      %h2
        = pluralize(@outcome.errors.count, "error")
        prohibited this outcome from being saved:
      %ul
        - @outcome.errors.full_messages.each do |msg|
          %li= msg
  .actions
    = f.submit
